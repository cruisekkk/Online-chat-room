import React, { Component } from "react";
import Loading from "../components/Loading"
class Room extends Component{
  render(){
    return(
      <div className="Room">
        <Loading/>
      </div>
    );
  }
}

export default Room;